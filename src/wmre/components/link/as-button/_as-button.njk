{% macro wmreLinkAsButton(params) %}
{# Imports #}
{% from "wmre/components/icon/_icon.njk" import wmreIcon %}
{# Set vars #}
{% set classNames = " " + params.classNames if params.classNames %} {# set params.classNames if any #}
{% set text = params.text if params.text else "Button text" %} {# set button text #}
{% set iconRight = params.iconRight if params.iconRight %} {# set icon right classes #}
{% set iconLeft = params.iconLeft if params.iconLeft %} {# set svg left reference #}
{% set link = params.link if params.link else "#" %} {# set link url #}
{% set linkTitle = params.linkTitle if params.linkTitle else "link title" %} {# set link title #}
{% set linkTarget = params.linkTarget if params.linkTarget else "_self" %} {# set link target #}

<a href="{{ link }}" title="{{ linkTitle }}" target="{{ linkTarget }}" class="wmre-btn{{ classNames }}">
  {#- iconLeft -#}
  {% if iconLeft %}
  {{
    wmreIcon({
      icon: iconLeft,
      class: 'wmre-btn__icon'
    }) | indent(2) | trim
  }}
  {{ text -}}
  {#- iconRight -#}
  {% elseif iconRight %}
  {{ text }}
  {{
    wmreIcon({
      icon: iconRight,
      class: 'wmre-btn__icon wmre-btn__icon--right'
    }) | indent(2) | trim
  }}

  {#- noIcon -#}
  {% else %}
  {{text}}
  {%- endif %}
</a>

{% endmacro %}