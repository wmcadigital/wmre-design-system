{% macro wmreLabel(params) %}
{# Set vars and defaults #}
{% set for = params.for if params.for else "genericInput" %}
{% set label = params.label if params.label else "Form label" %}

<label class="wmre-fe-label" for="{{for}}">{{ label }}</label>

{% endmacro %}