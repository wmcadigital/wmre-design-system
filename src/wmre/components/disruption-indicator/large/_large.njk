{% macro wmreDisruptionIndicatorLarge(params) %}
{% from "wmre/components/icon/_icon.njk" import wmreIcon %}

{% set strongText = params.strongText if params.strongText else "Indicator text" %} {# set button text #}
{% set iconRight = params.iconRight if params.iconRight %} {# set icon right classes #}
{% set iconLeft = params.iconLeft if params.iconLeft %} {# set svg left reference #}
{% set classModifier = ' wmre-disruption-indicator-large--' + params.classModifier if params.classModifier else "" %}

{{params.comment | safe}}
<div class="wmre-disruption-indicator-large{{classModifier}}">
  <div class="wmre-disruption-indicator-large__left-wrapper">
    <span class="wmre-disruption-indicator-large__left-icon-wrapper">
      {# Get SVG icon for mode #}
      {{-
        wmreIcon({
          icon: iconLeft,
          class: 'wmre-disruption-indicator-large__icon'
        }) | indent(2) | trim
      }} <br>{{params.mode}}
    </span>
    <span class="wmre-disruption-indicator-large__text">
      <strong>{{strongText}}</strong><br>
      {{- params.text }}
    </span>
  </div>

  {#- If params.iconRightBtn is true -#}
  {% if params.iconRightBtn %}
  <button class="wmre-disruption-indicator-large__btn" aria-label="Delete favourite - {{params.mode}} - {{ params.text }}">
    {# Get SVG icon #}
    {{-
      wmreIcon({
        icon: iconRight,
        class: 'wmre-disruption-indicator-large__icon'
      }) | indent(4) | trim
    }}
  </button>
  {#- else -#}
  {% else %}
  {#- Get SVG icon #}
  {{
    wmreIcon({
      icon: iconRight,
      class: 'wmre-disruption-indicator-large__icon wmre-disruption-indicator-large__icon--right'
    }) | indent(2) | trim
  -}}
  {% endif %}
</div>

{% endmacro %}