{% macro compExample(components, params) %}
{% from "wmre/components/button/_button.njk" import wmreButton %}
{% from "wmre/components/table/_table.njk" import wmreTable %}

{% set components = components if components else [wmreButton({text: "No component specified to example"})] %}
{% set showAsIframe = true if params.iframe else false %}
{% set componentPath = params.componentPath if params.componentPath else false %}

{% set njk = params.njk if params.njk else false %}
{% set js = params.js if params.js else false %}
{% set njkProps = params.njkProps if params.njkProps else false %}

<div class="wmre-website-code-example">
  {# Show a button to allow the user to see the component fullscreen #}
  {{
    wmreButton({
      classNames: 'wmre-btn--link wmre-m-md wmre-website-code-example__view-fullscreen',
      text: 'See this example in fullscreen'
    })
  }}
  <hr class="wmre-m-none" />


  {# PREVIEW COMPONENT #}
  <div class="wmre-website-code-example__inner-wrap">
    {# Loop through components passed in and collect in componentsToRender var #}
    {% set componentsToRender %}
      {%- for item in components %}
        {{item | trim}}
        {% if loop.last == false %}
          {% if params.displayInline %}
            <div class="wmre-m-b-md wmre-website-mar--right"></div>
          {% else %}
            <div class="wmre-m-b-md"></div>
          {% endif %}
        {% endif %}
      {% endfor -%}
    {% endset %}

    {# If iframe var set the show components in iframe #}
    {% if showAsIframe %}
      <iframe sandbox="allow-same-origin allow-scripts" title="Component example" class="wmre-website-code-example__iframe wmre-col-1" src="about:blank" data-src="{{componentsToRender | e | trim}}" frameBorder="0" loading="lazy" allowtransparency="true" scrolling="no" allow="fullscreen" width="100%" data-path="{{componentPath}}"></iframe>
    {% else %}
    {# Else show components like normal #}
      <div class="wmre-website-code-example__njk wmre-col-1">
        {{componentsToRender | safe}}
      </div>
    {% endif %}
  </div>
  {# END PREVIEW COMPONENT #}



  {# SHOW DETAIL TAB(S) #}

  {# HTML markup #}
  <hr class="wmre-m-none" />
  <details class="wmre-details">
    <summary class="wmre-link wmre-p-md">HTML markup</summary>
    {# Be careful with the below, the whitespace is extremely delicate (like a flower 🌸) #}
    {# The loop.revindex checks to see if last in array, if not add a linebreak (again, the fight for making nice whitespace continues) #}
<pre><code class="html" tabindex="0">{% for item in components -%}{{ item | forceescape | trim }}{% if loop.revindex !== 1 %}<br/>{% endif %}{% endfor -%}</code></pre>
  </details>

  {# Njk markup #}
  {% if njk and componentPath %}
    <hr class="wmre-m-none" />
    <details class="wmre-details">
      <summary class="wmre-link wmre-p-md">Nunjucks markup</summary>
{# Be careful with the below, the whitespace is extremely delicate (like a flower 🌸) #}
<pre><code class="njk" tabindex="0">{%- include componentPath + "_example.njk" -%}</code></pre>
    </details>
  {% endif %}

  {# Njk props #}
  {% if njkProps %}
    <hr class="wmre-m-none" />
    <details class="wmre-details">
      <summary class="wmre-link wmre-p-md">Nunjucks properties</summary>
      <div class="wmre-p-md">
        <table class="wmre-table wmre-p-md">
          <thead>
            <tr>
              <th scope="col">Name</th>
              <th scope="col">Type</th>
              <th scope="col">Description</th>
            </tr>
          </thead>
          <tbody>
            {% for prop in njkProps %}
              <tr>
                <th scope="row" data-header="Name">{{prop.name}}</th>
                <td data-header="Type">{{prop.type}}</td>
                <td data-header="Description">{{prop.description | safe}}</td>
              </tr>
            {% endfor %}
          </tbody>
        </table>
      </div>
    </details>
  {% endif %}

  {# JS markup #}
  {% if js and componentPath %}
    <div class="wmre-website-code-example__js">
      {# HIDING THIS SECTION until we can compile ES5 for IE11 #}
      {# Show recommended JS (ES2015) #}
      {# <hr class="wmre-m-none" />
      <details class="wmre-details wmre-details--js">
        <summary class="wmre-link wmre-p-md">Recommended javascript (browser compatible)</summary>
<pre><code class="js" tabindex="0"></code></pre>
      </details> #}

      {# Show recommended JS (ES6) #}
      <hr class="wmre-m-none" />
      <details class="wmre-details wmre-details--es6">
        <summary class="wmre-link wmre-p-md">Recommended javascript (ES6)</summary>
<pre><code class="js" tabindex="0"></code></pre>
      </details>

      <script type="text/plain" class="wmre-website-code-example__script">
        {%- include componentPath + "_example.js" -%}
      </script>
    </div>
  {% endif %}
</div>

{# END SHOW DETAIL TAB(S) #}

<hr>
{% endmacro %}
